# Local development only: app + PostgreSQL 16 in one command.
services:
  app:
    build: .
    ports: ['4000:4000']
    environment:
      NODE_ENV: development
      PORT: 4000
      DATABASE_URL: postgresql://postgres:postgres@db:5432/afro_broker
      RUN_MIGRATIONS_ON_BOOT: 'true'
      BROKING_ENABLED: 'true'
      BROKING_LLOYDS_BROKER_NO: ${BROKING_LLOYDS_BROKER_NO:-0621}
      ALLOW_DEMO_AUTH: 'true'
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:4000}
    depends_on:
      db:
        condition: service_healthy
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: afro_broker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ['5432:5432']
    volumes: ['postgres_data:/var/lib/postgresql/data']
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  postgres_data:
